server {
    listen 0.0.0.0:${PORT} default_server;
    listen [::]:${PORT} default_server;
    
    server_name _;
    charset utf-8;
    
    root /var/www/app/public;
    index index.php;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # Symfony routing - only check files, not directories
    location / {
        try_files $uri /index.php$is_args$args;
    }

    # PHP-FPM gateway - explicitly match index.php
    location = /index.php {
        # Timeouts for Railway slow startups
        fastcgi_read_timeout 300s;
        fastcgi_connect_timeout 30s;
        fastcgi_send_timeout 300s;

        fastcgi_pass 127.0.0.1:9000;
        fastcgi_split_path_info ^(.+\.php)(/.*)$;
        include fastcgi_params;
        
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $document_root;
        
        # Buffering for large responses
        fastcgi_buffer_size 128k;
        fastcgi_buffers 4 256k;
        fastcgi_busy_buffers_size 256k;
        fastcgi_max_temp_file_size 2m;
    }

    # Handle PATH_INFO for Symfony routing
    location ~ ^/index\.php(/|$) {
        # Timeouts for Railway slow startups
        fastcgi_read_timeout 300s;
        fastcgi_connect_timeout 30s;
        fastcgi_send_timeout 300s;

        fastcgi_pass 127.0.0.1:9000;
        fastcgi_split_path_info ^(.+\.php)(/.*)$;
        include fastcgi_params;
        
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $document_root;
        
        # Buffering for large responses
        fastcgi_buffer_size 128k;
        fastcgi_buffers 4 256k;
        fastcgi_busy_buffers_size 256k;
        fastcgi_max_temp_file_size 2m;
        
        internal;
    }

    # Prevent direct PHP file access except index.php
    location ~ \.php$ {
        return 404;
    }

    # Static files caching
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Deny access to hidden files
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
}
