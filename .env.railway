# Production environment configuration for Railway
APP_ENV=prod
APP_DEBUG=0

# ============================================================================
# CRITICAL: Set these in Railway Environment Variables (Web Service Settings)
# ============================================================================
# APP_SECRET: Run locally: php -r 'echo bin2hex(random_bytes(32));'
#   Generate 32-byte hex string, set in Railway dashboard → Variables
# 
# DATABASE_URL: (Optional - defaults to SQLite)
#   SQLite (default): sqlite:///%kernel.project_dir%/var/data/school_management_prod.db
#   PostgreSQL: postgresql://user:pass@host:5432/dbname
#   MySQL: mysql://user:pass@host:3306/dbname
# ============================================================================

# DO NOT HARDCODE SECRETS HERE - Railway injected values override these
APP_SECRET=MISSING-PLEASE-SET-IN-RAILWAY-VARIABLES
DEFAULT_URI=https://example.railway.app
DATABASE_URL=sqlite:///%kernel.project_dir%/var/data/school_management_prod.db
