# Production environment configuration for Railway
APP_ENV=prod
APP_DEBUG=0

# You MUST set these in Railway Environment Variables panel:
# - APP_SECRET: Generate with: php -r 'echo bin2hex(random_bytes(16));'
# - DATABASE_URL: Default is SQLite, but can use: mysql://, postgresql://, etc.

# Default values that can be overridden:
APP_SECRET=missing-configure-in-railway
DEFAULT_URI=https://school-management-production-1378.up.railway.app
DATABASE_URL=sqlite:///%kernel.project_dir%/var/data/school_management_prod.db
